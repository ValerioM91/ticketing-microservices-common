{
  "name": "@valeriom91-org/common",
  "version": "1.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/**/*",
    "build": "pnpm clean && tsc",
    "pub": "git add . && git commit -m \"updates\" && npm version patch && pnpm build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.1.0",
    "typescript": "^5.3.3"
  }
}
